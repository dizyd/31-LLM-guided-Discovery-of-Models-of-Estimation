{"model_id": "d3653bdd779d", "tag": "Mammals_GCM_run0", "source": "\ndef _softplus(x):\n    return np.log1p(np.exp(np.clip(x, -30.0, 30.0)))\n\n\ndef _softmax(x):\n    z = np.asarray(x, dtype=float) - np.max(x)\n    e = np.exp(z)\n    return e / e.sum()\n\nNUM_PARAMETERS = 10\n\ndef model(parameters, cues, ex_cues=None, ex_crit=None):\n    \"\"\"\n    Compute predicted criterion estimate.\n\n    Parameters\n    ----------\n    parameters : np.ndarray of shape (num_parameters,)\n        Unconstrained model parameters, of order 1.\n\n    cues : np.ndarray of shape (n_trials, n_dim)\n        Feature matrix of stimuli across trials.\n\n    ex_cues : np.ndarray of shape (n_exemplars, n_dim)\n        Feature matrix of the learned exemplars.\n\n    ex_crit : np.ndarray of shape (n_exemplars,)\n        Criterion values of the learned exemplars.\n\n    Returns\n    -------\n    pred_crit : np.ndarray of shape (n_trials,)\n        Predicted criterion estimates for each trial, on the raw criterion scale.\n    \"\"\"\n    # Generalized Context Model: a similarity-weighted average of the exemplar criteria.\n    n_dim = cues.shape[1]\n    p     = 2.0\n\n    # Reference distance, so that parameters[0] == 0 is a sensible sensitivity.\n    ex_gap = np.abs(ex_cues[:, None, :] - ex_cues[None, :, :]) ** p\n    ex_gap = (ex_gap.sum(axis=-1)) ** (1.0 / p)\n    d_ref  = np.median(ex_gap[ex_gap > 0]) + 1e-9\n\n    # Sensitivity: positive by construction.\n    c = np.exp(np.clip(parameters[0], -20.0, 20.0)) / d_ref\n\n    # Attention weights: non-negative and summing to n_dim, with n_dim - 1 free logits\n    # (the n_dim-th is fixed at 0, which is what makes the set identified).\n    logits = np.concatenate([np.zeros(1), parameters[1:n_dim]])\n    w      = _softmax(logits) * n_dim\n\n    diff      = np.abs(cues[:, None, :] - ex_cues[None, :, :]) ** p\n    distances = ((diff * w[None, None, :]).sum(axis=-1)) ** (1.0 / p)\n    sim       = np.exp(-c * distances)\n\n    sim_sum   = np.maximum(sim.sum(axis=1), 1e-12)\n    pred_crit = (sim * ex_crit[None, :]).sum(axis=1) / sim_sum\n\n    return pred_crit\n", "k": 10, "cv_ll": -20421.438905873565, "aic": 36222.90186586097, "iteration": 0, "seed": "GCM", "run_id": 0, "domain": "Mammals"}
{"model_id": "1234bf27aa26", "tag": "Mammals_GCM_run0", "source": "import numpy as np\n\ndef _softplus(x):\n    return np.log1p(np.exp(np.clip(x, -30.0, 30.0)))\n\ndef _softmax(x):\n    z = np.asarray(x, dtype=float) - np.max(x)\n    e = np.exp(z)\n    return e / e.sum()\n\nNUM_PARAMETERS = 11\n\ndef model(parameters, cues, ex_cues=None, ex_crit=None):\n    \"\"\"\n    Compute predicted criterion estimate with log-scale transformation and global scaling.\n    \"\"\"\n    n_dim = cues.shape[1]\n    p = 2.0\n\n    ex_gap = np.abs(ex_cues[:, None, :] - ex_cues[None, :, :]) ** p\n    ex_gap = (ex_gap.sum(axis=-1)) ** (1.0 / p)\n    d_ref = np.median(ex_gap[ex_gap > 0]) + 1e-9\n\n    c = np.exp(np.clip(parameters[0], -20.0, 20.0)) / d_ref\n\n    logits = np.concatenate([np.zeros(1), parameters[1:n_dim]])\n    w = _softmax(logits) * n_dim\n\n    diff = np.abs(cues[:, None, :] - ex_cues[None, :, :]) ** p\n    distances = ((diff * w[None, None, :]).sum(axis=-1)) ** (1.0 / p)\n    sim = np.exp(-c * distances)\n\n    sim_sum = np.maximum(sim.sum(axis=1), 1e-12)\n    log_ex_crit = np.log(ex_crit)\n    log_pred = (sim * log_ex_crit[None, :]).sum(axis=1) / sim_sum\n    gamma = np.exp(np.clip(parameters[10], -10.0, 10.0))\n    pred_crit = np.exp(log_pred) * gamma\n\n    return pred_crit", "k": 11, "cv_ll": -20680.111406255663, "aic": 36179.148861685484, "iteration": 1, "seed": "GCM", "run_id": 0, "domain": "Mammals"}
{"model_id": "4959a14cc441", "tag": "Mammals_GCM_run0", "source": "NUM_PARAMETERS = 11\n\ndef model(parameters, cues, ex_cues=None, ex_crit=None):\n    \"\"\"\n    Compute predicted criterion estimate with distance-based extrapolation adjustment.\n    \"\"\"\n    n_dim = cues.shape[1]\n    p = 2.0\n\n    # Reference distance\n    ex_gap = np.abs(ex_cues[:, None, :] - ex_cues[None, :, :]) ** p\n    ex_gap = (ex_gap.sum(axis=-1)) ** (1.0 / p)\n    d_ref  = np.median(ex_gap[ex_gap > 0]) + 1e-9\n\n    # Sensitivity\n    c = np.exp(np.clip(parameters[0], -20.0, 20.0)) / d_ref\n\n    # Attention weights\n    logits = np.concatenate([np.zeros(1), parameters[1:n_dim]])\n    logits_max = np.max(logits)\n    logits_stable = logits - logits_max\n    w_exp = np.exp(logits_stable)\n    w = (w_exp / np.sum(w_exp)) * n_dim\n\n    diff      = np.abs(cues[:, None, :] - ex_cues[None, :, :]) ** p\n    distances = ((diff * w[None, None, :]).sum(axis=-1)) ** (1.0 / p)\n    sim       = np.exp(-c * distances)\n\n    sim_sum   = np.maximum(sim.sum(axis=1), 1e-12)\n    pred_crit = (sim * ex_crit[None, :]).sum(axis=1) / sim_sum\n\n    # Distance-based extrapolation adjustment\n    min_distance = np.min(distances, axis=1)\n    beta = parameters[10]\n    pred_crit += beta * min_distance\n\n    return pred_crit", "k": 11, "cv_ll": -20243.15527154954, "aic": 36179.99553404955, "iteration": 2, "seed": "GCM", "run_id": 0, "domain": "Mammals"}
{"model_id": "83f8c3c40d9a", "tag": "Mammals_GCM_run0", "source": "NUM_PARAMETERS = 11\n\ndef model(parameters, cues, ex_cues=None, ex_crit=None):\n    \"\"\"\n    Compute predicted criterion estimate with distance-based extrapolation adjustment.\n    \"\"\"\n    n_dim = cues.shape[1]\n    p = 2.0\n\n    # Reference distance\n    ex_gap = np.abs(ex_cues[:, None, :] - ex_cues[None, :, :]) ** p\n    ex_gap = (ex_gap.sum(axis=-1)) ** (1.0 / p)\n    d_ref  = np.median(ex_gap[ex_gap > 0]) + 1e-9\n\n    # Sensitivity\n    c = np.exp(np.clip(parameters[0], -20.0, 20.0)) / d_ref\n\n    # Attention weights\n    logits = np.concatenate([np.zeros(1), parameters[1:n_dim]])\n    logits_max = np.max(logits)\n    logits_stable = logits - logits_max\n    w_exp = np.exp(logits_stable)\n    w = (w_exp / np.sum(w_exp)) * n_dim\n\n    diff      = np.abs(cues[:, None, :] - ex_cues[None, :, :]) ** p\n    distances = ((diff * w[None, None, :]).sum(axis=-1)) ** (1.0 / p)\n    sim       = np.exp(-c * distances)\n\n    sim_sum   = np.maximum(sim.sum(axis=1), 1e-12)\n    pred_crit = (sim * ex_crit[None, :]).sum(axis=1) / sim_sum\n\n    # Directional extrapolation adjustment\n    nearest_exemplar_indices = np.argmin(distances, axis=1)\n    nearest_exemplar_cues = ex_cues[nearest_exemplar_indices]\n    delta = cues - nearest_exemplar_cues\n    beta = parameters[10]\n    pred_crit += beta * np.sum(w * delta, axis=1)\n\n    return pred_crit", "k": 11, "cv_ll": -20654.15424530722, "aic": 36217.87087216499, "iteration": 3, "seed": "GCM", "run_id": 0, "domain": "Mammals"}
{"model_id": "81c7966ac83d", "tag": "Mammals_GCM_run0", "source": "import numpy as np\n\nNUM_PARAMETERS = 11\n\ndef model(parameters, cues, ex_cues=None, ex_crit=None):\n    \"\"\"\n    Compute predicted criterion estimate with distance-based extrapolation adjustment.\n    \"\"\"\n    n_dim = cues.shape[1]\n    p = 2.0\n\n    # Reference distance\n    ex_gap = np.abs(ex_cues[:, None, :] - ex_cues[None, :, :]) ** p\n    ex_gap = (ex_gap.sum(axis=-1)) ** (1.0 / p)\n    d_ref  = np.median(ex_gap[ex_gap > 0]) + 1e-9\n\n    # Sensitivity\n    c = np.exp(np.clip(parameters[0], -20.0, 20.0)) / d_ref\n\n    # Attention weights\n    logits = np.concatenate([np.zeros(1), parameters[1:n_dim]])\n    logits_max = np.max(logits)\n    logits_stable = logits - logits_max\n    w_exp = np.exp(logits_stable)\n    w = (w_exp / np.sum(w_exp)) * n_dim\n\n    diff      = np.abs(cues[:, None, :] - ex_cues[None, :, :]) ** p\n    distances = ((diff * w[None, None, :]).sum(axis=-1)) ** (1.0 / p)\n    sim       = np.exp(-c * distances)\n\n    sim_sum   = np.maximum(sim.sum(axis=1), 1e-12)\n    pred_crit = (sim * ex_crit[None, :]).sum(axis=1) / sim_sum\n\n    # Distance-based extrapolation adjustment in log space\n    min_distance = np.min(distances, axis=1)\n    beta = parameters[10]\n    pred_crit = np.log(pred_crit) + beta * min_distance\n    pred_crit = np.exp(pred_crit)\n\n    return pred_crit", "k": 11, "cv_ll": -20534.089733395347, "aic": 36153.79214756697, "iteration": 4, "seed": "GCM", "run_id": 0, "domain": "Mammals"}
